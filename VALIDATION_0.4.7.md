# Проверки Story Manager · Niya Custom 0.4.7

- `57/57` автоматических тестов.
- Синтаксис JavaScript проверен через `node --check`.
- Автосаммари: точные N видимых источников, safety tail 1, отсутствие зависимости от автора.
- Frontier: дальняя ручная карточка не перескакивает дыру; перед островом создаётся partial-карточка.
- Integrity: Hide/Unhide во время автоматического запроса меняет план до сохранения; готовая карточка не инвалидируется обычным Hide.
- Transport: выбранный профиль использует `ConnectionManagerRequestService`; Custom использует streaming; после ошибки нет fallback или повторной платной попытки.
- Ручной диапазон после непокрытой истории показывает предупреждение.

Реальный провайдер и мобильный интерфейс требуют короткой ручной проверки по `TESTING_0.4.7.md`.
