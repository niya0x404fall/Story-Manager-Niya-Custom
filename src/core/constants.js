/** Путь расширения для renderExtensionTemplateAsync (без угадывания по URL). */
export const path = "third-party/Story-Manager-Niya-Custom";
export const templates_path = path;

/** Имена HTML-фрагментов в папке templates/ (без .html). */
export const template_files = [
  "templates/settings",
  "templates/popups",
  "templates/entities",
  "templates/message-buttons",
];
